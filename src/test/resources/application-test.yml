#------------------------------------
# Spring test settings
#------------------------------------

spring:
  profiles:
    active: test
  datasource:
    jdbc-url: jdbc:h2:mem:nextbank_migration
    url: jdbc:h2:mem:nextbank_migration
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false